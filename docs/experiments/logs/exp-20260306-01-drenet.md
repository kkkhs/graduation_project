# 实验记录：DRENet 首轮（2026-03-06）

## 1. 基本信息
- 实验编号：`exp-20260306-01-drenet`
- 日期：2026-03-06
- 模型：DRENet
- 代码版本（commit）：`a187dbe0f623b521a62c6176c7cafaa7322f5f66`
- 数据版本：LEVIR-Ship（`E:/datasets/LEVIR-Ship`）
- 随机种子：`<待填>`

## 2. 训练配置
- 输入尺寸：`<待填>`
- batch size：`4`（OOM 时降到 `2`）
- epoch：`300`
- 优化器与学习率策略：`<按实际输出填写>`
- 数据增强：`<待填>`
- 后处理参数：`conf=0.25, iou=0.50`

## 3. 环境与资源
- GPU / 显存：RTX 3060 6GB
- CPU / 内存：`<待填>`
- 训练总耗时：`<待填>`
- 推理速度（FPS）：`<待填>`

## 4. 结果（定量）
- AP50：`<待填>`
- Precision：`<待填>`
- Recall：`<待填>`
- F1：`<待填>`
- 备注（是否 early stop、是否异常）：`<待填>`

## 5. 结果（定性）
- 成功案例路径：`<待填>`
- 漏检案例路径：`<待填>`
- 误检案例路径：`<待填>`
- 复杂场景说明（碎云/强浪/近岸）：`<待填>`

## 6. 结论
- 本次实验结论（1-3条）：
  1. `<待填>`
  2. `<待填>`
  3. `<待填>`
- 与上次实验差异：`首轮基线`
- 下一步动作：`跑 MMDet/YOLO 冒烟并回填 baselines 表`

## 7. 复现命令
```powershell
# train
cd E:\work\DRENet
python train.py --cfg .\models\DRENet.yaml --epochs 300 --workers 4 --batch-size 4 --device 0 --project .\runs\drenet_main --data .\data\ship.yaml

# eval
python test.py --weights .\runs\drenet_main\exp\weights\best.pt --project .\runs\drenet_eval --device 0 --batch-size 4 --data .\data\ship.yaml

# infer / visualize
python detect.py --weights .\runs\drenet_main\exp\weights\best.pt --source E:\datasets\LEVIR-Ship\test\images --device 0
```

